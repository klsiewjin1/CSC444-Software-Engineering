<% provide(:title, 'Sign Up') %>

<h1>Sign up</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_for @user, :html=> {:id => 'registrationForm'} do |f| %>
      <div class="tab">
       <h2>The Basics</h2>
      <%= f.text_field :username, placeholder: "Username", onfocusout: "validateInput(this)" %>
      <br><br>
      <%= f.select :user_type, [['Client', 'client'], ['Teenager', 'teenager']] %>
      </div>
      
      <div class="tab" style = "display:none">
      <h2>Tell us about yourself</h2>
      <%= f.text_field :fname, placeholder: "First name", onfocusout: "validateInput(this)" %>
      <br>
      <%= f.text_field :lname, placeholder: "Last name", onfocusout: "validateInput(this)" %>
      </div>

      <div class="tab"  style="display:none">
      <%= f.email_field :email, placeholder: "Email", onfocusout: "validateInput(this)"%>
      <%= f.email_field :email_confirmation, placeholder: "Confirm email", onfocusout: "validateInput(this)" %>
      </div>

      <div class="tab"  style="display:none">
      <%= f.password_field :password, placeholder: "Password", onfocusout: "validateInput(this)" %>
      <%= f.password_field :password_confirmation, placeholder: "Confirm password", onfocusout: "validateInput(this)" %>
      </div>

      <br>
      <div style="overflow:auto;">
        <div style="float:right;">
          <button type="button" id="prevBtn" onclick="nextPrev(-1)" class="btn btn-primary">Previous</button>
          <button type="button" id="nextBtn" onclick="nextPrev(1)" class="btn btn-primary">Next</button>
          <%= f.submit "Create Account", class: "btn btn-primary", id: "submitBtn", style: "display:none" %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= link_to 'Back', users_path %>
