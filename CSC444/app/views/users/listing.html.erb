<% @is_teen = user_is_teen(@current_user) %>
<% @is_client = user_is_client(@current_user) %>
<% if @is_teen %>
	<%= render "applications" %>
<% elsif @is_client %>
	<%= render "posted" %>
<% end %>
<% if @is_teen or @is_client %>
	<%= render "approved" %>
<% end %>